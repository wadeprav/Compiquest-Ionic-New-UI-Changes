import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.compiquest.app',
  appName: 'CompiQuest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
